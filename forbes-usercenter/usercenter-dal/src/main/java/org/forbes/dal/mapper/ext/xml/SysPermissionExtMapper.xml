<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.forbes.dal.mapper.ext.SysPermissionExtMapper" >
  <resultMap id="BaseResultMap" type="org.forbes.dal.entity.SysPermission" extends="org.forbes.dal.mapper.SysPermissionMapper.BaseResultMap">
  </resultMap>
  <sql id="Base_Column_List" >
   <include refid="org.forbes.dal.mapper.SysPermissionMapper.Base_Column_List" />
  </sql>

<!--查询一个角色的所有权限-->
    <select id="getPermissionByRoleId" resultMap="BaseResultMap" parameterType="Integer">
        SELECT
        p.parent_id,
        p.name,
        p.perms,
        p.icon,
        p.component,
        p.component_name,
        p.always_show,
        p.is_route,
        p.is_leaf,
        p.is_hidden,
        p.sort_no,
        p.description,
        p.redirect,
        p.url
       FROM f_sys_permission p
       JOIN f_sys_role_permission rp on p.id = rp.permission_id
	   WHERE rp.role_id =#{roleId}
    </select>
    <!--查询所有角色与其对应的所有权限-->
    <select id="getPermissionByRole" resultMap="BaseResultMap">
        SELECT
        p.parent_id,
        p.name,
        p.perms,
        p.icon,
        p.component,
        p.component_name,
        p.always_show,
        p.is_route,
        p.is_leaf,
        p.is_hidden,
        p.sort_no,
        p.description,
        p.redirect,
        p.url,
		r.id,
        r.role_name,
        r.role_code
        FROM f_sys_permission p
        JOIN f_sys_role_permission rp
        ON rp.role_id = p.id JOIN f_sys_role r
        ON rp.role_id = r.id
    </select>
    <!--给一个角色添加权限-->
    <insert id="addPermissionToRole" parameterType="org.forbes.comm.dto.RolePermissionDto">
      INSERT INTO f_sys_role_permission
      (role_id,permission_id)
      VALUES (#{roleId,jdbcType=BIGINT},#{permissionId,jdbcType=BIGINT})
    </insert>
    <!--添加一个新的权限(仅添加权限)-->
    <insert id="addPermission" parameterType="org.forbes.dal.entity.SysPermission">
        INSERT INTO f_sys_permission
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="parentId != null">
                parent_id,
            </if>
            <if test="name != null">
                name,
            </if>
            <if test="perms != null">
                perms,
            </if>
            <if test="component != null">
                component,
            </if>
            <if test="componentName != null">
                component_name,
            </if>
            <if test="isRoute != null">
                is_route,
            </if>
            <if test="isLeaf != null">
                is_ieaf,
            </if>
            <if test="isHidden != null">
                is_hidden,
            </if>
            <if test="sortNo != null">
                sort_no,
            </if>
            <if test="description != null">
                description,
            </if>
            <if test="redirect != null">
                redirect,
            </if>
            <if test="url != null">
                url,
            </if>
            <if test="icon != null">
                icon,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="parentId != null">
                #{parentId},
            </if>
            <if test="name != null">
                #{name},
            </if>
            <if test="perms != null">
                #{perms},
            </if>
            <if test="component != null">
                #{component},
            </if>
            <if test="componentName != null">
                #{componentName},
            </if>
            <if test="isRoute != null">
                #{isRoute},
            </if>
            <if test="isLeaf != null">
                #{isLeaf},
            </if>
            <if test="isHidden != null">
                #{isHidden},
            </if>
            <if test="sortNo != null">
                #{sortNo},
            </if>
            <if test="description != null">
                #{description},
            </if>
            <if test="redirect != null">
                #{redirect},
            </if>
            <if test="url != null">
                #{url},
            </if>
            <if test="icon != null">
                #{icon},
            </if>
        </trim>
    </insert>
    <!--修改权限内容-->
    <update id="updatePermissionById" parameterType="org.forbes.comm.dto.UpdatePermissionDto">
      UPDATE f_sys_permission SET
        parent_id = #{parentId},
        name = #{name},
        description=#{description}
        WHERE id = #{permissionId,jdbcType=BIGINT}
    </update>
    <!--修改角色的一个权限(中间表)-->
    <update id="updatePermissionToRole" parameterType="Long">
        UPDATE f_sys_role_permission
        SET permission_id = #{permissionId,jdbcType=BIGINT}
        WHERE role_id = #{roleId,jdbcType=BIGINT} AND id = #{id,jdbcType=BIGINT}
    </update>
    <!--删除角色的一个权限(中间表)-->
    <delete id="deletePermissionToRole" parameterType="Long">
        DELETE FROM f_sys_role_permission
        WHERE role_id = #{roleId,jdbcType=BIGINT} AND permission_id = #{permissionId,jdbcType=BIGINT}
    </delete>
</mapper>